diff --git a/node_modules/appcenter-crashes/ios/appcenter-crashes.podspec b/node_modules/appcenter-crashes/appcenter-crashes.podspec
similarity index 85%
rename from node_modules/appcenter-crashes/ios/appcenter-crashes.podspec
rename to node_modules/appcenter-crashes/appcenter-crashes.podspec
index fa37116..9cde8ed 100644
--- a/node_modules/appcenter-crashes/ios/appcenter-crashes.podspec
+++ b/node_modules/appcenter-crashes/appcenter-crashes.podspec
@@ -1,6 +1,6 @@
 require 'json'

-package = JSON.parse(File.read(File.join(__dir__, '../package.json')))
+package = JSON.parse(File.read(File.join(__dir__, './package.json')))

 Pod::Spec.new do |s|
   s.name              = package['name']
@@ -13,7 +13,7 @@ Pod::Spec.new do |s|
   s.author            = { 'Microsoft' => 'appcentersdk@microsoft.com' }

   s.source            = { :git => "https://github.com/microsoft/appcenter-sdk-react-native.git" }
-  s.source_files      = "AppCenterReactNativeCrashes/**/*.{h,m}"
+  s.source_files      = "ios/AppCenterReactNativeCrashes/**/*.{h,m}"
   s.platform          = :ios, '9.0'
   s.requires_arc      = true

diff --git a/node_modules/appcenter-crashes/react-native.config.js b/node_modules/appcenter-crashes/react-native.config.js
index 6b9b1de..1da90b5 100644
--- a/node_modules/appcenter-crashes/react-native.config.js
+++ b/node_modules/appcenter-crashes/react-native.config.js
@@ -1,11 +1,7 @@
-const path = require('path');
-
 module.exports = {
   dependency: {
     platforms: {
-      ios: {
-        podspecPath: path.join(__dirname, 'ios', 'appcenter-crashes.podspec')
-      },
+      ios: {},
       android: {
         packageInstance: 'new AppCenterReactNativeCrashesPackage(getApplication(), getResources().getString(R.string.appCenterCrashes_whenToSendCrashes))'
       }
